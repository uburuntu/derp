[Unit]
Description=PostgreSQL Daily Backup
After=postgresql.service

[Service]
Type=oneshot
User=postgres
Group=postgres
ExecStart=/usr/local/bin/pg_backup --type daily
Environment=PG_BACKUP_DIR=/var/backups/postgresql

[Install]
WantedBy=multi-user.target
